// This file will be expanded in later tasks
// For now, it's a placeholder to allow Prisma to generate

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
